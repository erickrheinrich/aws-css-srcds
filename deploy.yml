# - hosts: localhost
#   connection: local
#   roles:
#     - role: infra
#   tags: ["infrastructure"]

# - hosts: localhost
#   connection: local
#   roles:
#     - role: upload-css-files
#   tags: ["upload-css-files"]

- hosts: localhost
  connection: local
  roles:
    - role: css-server-instance
  tags: ["css-server-instance"]
